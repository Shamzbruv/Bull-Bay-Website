import { getOrganizationId,getUserPermissions } from "@/lib/auth/session";
import { AccessDenied } from "@/components/access-denied";
import { OfficeActionForm } from "@/components/office-action-form";
import { googleConfig } from "@/lib/calendar/integrations";
import { pushConfig } from "@/lib/push/server";
import { saveGoogleConfig,enablePhoneNotifications } from "./actions";
export default async function IntegrationsPage(){const org=await getOrganizationId();if(!org||!(await getUserPermissions(org)).has('integrations.manage'))return <AccessDenied/>;const config=await googleConfig();const push=await pushConfig();return <><div className="dashboard-header"><h1>Calendar & phone setup</h1></div><section className="panel"><h2>Google Calendar</h2><p>Status: {config.clientId&&config.clientSecret?'Configured':'Setup required'}</p><ol><li>In Google Cloud, enable the Google Calendar API.</li><li>Configure Google Auth Platform branding and audience for the church. Publish the app when ready for members.</li><li>Create an OAuth client with application type “Web application”.</li><li>Add this authorized redirect URI: <code>{config.redirectUri}</code></li><li>Paste the client ID and secret below. Users then connect from Calendar connections.</li></ol><OfficeActionForm action={saveGoogleConfig} label="Save Google configuration"><label>Client ID<input name="client_id" required defaultValue={config.clientId??''} autoComplete="off"/></label><label>Client secret<input name="client_secret" type="password" required autoComplete="new-password"/></label></OfficeActionForm><p className="form-note">Client secrets and Google refresh tokens are encrypted and kept on the server. The requested calendar scope only manages calendars created by this app.</p></section><section className="panel"><h2>Phone notifications</h2><p>{push?'Push notifications are configured. Users can opt in in their notification settings.':'Enable Web Push so members can opt in on their phones and browsers.'}</p>{!push&&<OfficeActionForm action={enablePhoneNotifications} label="Enable phone notifications"/>}</section></>;}
